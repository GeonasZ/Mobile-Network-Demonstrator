extends ColorRect

#extends Polygon2D
#
#var length = 200
#var width = 200
#
## Called when the node enters the scene tree for the first time.
#func _ready() -> void:
	#self.position = Vector2(0,0)
	#self.polygon = [Vector2(0,0),Vector2(length,0),
					#Vector2(length,width),Vector2(0,width)]
	#self.color = Color8(255,255,255)
	##self.color.a = 0.5
#
#
## Called every frame. 'delta' is the elapsed time since the previous frame.
#func _process(delta: float) -> void:
	#pass
